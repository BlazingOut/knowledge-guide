# AI 应用开发秋招知识地图

## 实现方案

## 1. 实现目标

第一版实现一个面向个人使用的静态 Web App，核心流程是：

```text
JavaGuide 原始目录
        ↓
知识地图首页
        ↓
分类详情页
        ↓
知识点学习打卡
        ↓
学习状态与进度统计
```

产品保留 JavaGuide 的原始目录结构。应用只保存知识点的标题、层级、优先级和外部链接，不复制 JavaGuide 的文章正文。

## 2. 技术选型

- React：构建页面和交互组件。
- TypeScript：约束知识地图、学习状态和页面组件的数据结构。
- Vite：提供开发服务器和生产构建能力。
- React Router：实现首页和分类详情页路由。
- Tailwind CSS：实现设计图中的布局、颜色、间距和响应式样式。
- Lucide React：提供导航、状态和操作图标。
- localStorage：保存用户的学习状态、面试验证、面试暴露和笔记。

第一版不引入后端、数据库、账号系统或云端同步。

## 3. 页面结构

### 3.1 首页 `/`

对应设计图上半部分，包含：

- 产品标题和当前准备阶段；
- 知识覆盖率、可回答率、面试验证率；
- 一级知识分类卡片；
- 每个分类的知识点数量和完成进度；
- 轻量的今日目标、最近面试暴露和投递线提示。

首页的主入口是知识分类，不按照“已学习 / 未学习”重新组织内容。

### 3.2 分类详情页 `/category/:categoryId`

对应设计图下半部分，包含：

- 左侧 JavaGuide 原始目录；
- 中间当前目录下的知识点列表；
- 知识点优先级；
- 学习状态切换；
- 面试验证和面试暴露标记；
- JavaGuide 外部文章链接；
- 搜索和快速筛选；
- 右侧今日目标和学习辅助区域。

外部链接使用新标签页打开，避免用户离开当前学习页面。

## 4. 知识数据模型

知识地图使用独立的 TypeScript 数据文件管理，保持“分类 → 目录 → 知识点”的层级：

```ts
type Priority = "must" | "high" | "know" | "skip";
type TopicStatus = "unlearned" | "seen" | "answerable";

interface KnowledgePoint {
  id: string;
  title: string;
  url?: string;
  priority: Priority;
}

interface KnowledgeSection {
  id: string;
  title: string;
  children?: KnowledgeSection[];
  topics?: KnowledgePoint[];
}

interface KnowledgeCategory {
  id: string;
  title: string;
  description?: string;
  sections: KnowledgeSection[];
}
```

初始数据按照 JavaGuide 原始结构整理，例如：

```text
计算机基础
├── 网络
├── 操作系统
├── 数据结构
└── 算法
```

进入具体分类后，再展示面试题、基础、应用层、传输层、网络层、安全等下级目录。

## 5. JavaGuide HTML 数据整理

开发阶段使用本地 HTML 作为资料源，提取：

- `title` 页面标题；
- `h1` 到 `h6` 标题层级；
- `a[href]` 文章链接；
- 链接文字与所属目录。

整理结果写入项目的数据文件。运行中的前端不解析 HTML，也不依赖爬虫或网络请求。

这样可以让知识地图保持稳定，同时方便后续手动调整课时名称、优先级和文章链接。

## 6. 用户状态与持久化

原始知识数据和用户状态分离：

```ts
interface UserTopicState {
  status: TopicStatus;
  interviewVerified: boolean;
  interviewExposed: boolean;
  note: string;
  addedToToday: boolean;
}
```

`localStorage` 保存一个版本化的数据对象，例如：

```text
knowledge-guide:user-state:v1
```

状态变化后立即保存，并触发首页、分类页和进度组件重新计算。

## 7. 进度计算

- 知识覆盖率：状态为 `seen` 或 `answerable` 的知识点 / 总知识点；
- 可回答率：状态为 `answerable` 的知识点 / 总知识点；
- 面试验证率：标记为 `interviewVerified` 的知识点 / 总知识点；
- 分类进度：同样按当前分类内的知识点计算。

这些指标只反映准备状态，不代表获得 offer 的概率。

## 8. 推荐项目目录

```text
.
├── assets/
│   ├── 前端设计图.png
│   ├── AI应用开发秋招知识地图_设计方案_v1.0.md
│   ├── 计算机基础知识体系...JavaGuide.html
│   └── 计算机基础知识体系...JavaGuide_files/
├── 实现方案.md
├── package.json
├── vite.config.ts
└── src/
    ├── components/
    │   ├── Layout/
    │   ├── CategoryCard/
    │   ├── TopicList/
    │   ├── TopicDetail/
    │   └── Progress/
    ├── pages/
    │   ├── HomePage.tsx
    │   └── CategoryPage.tsx
    ├── data/
    │   ├── categories.ts
    │   └── knowledge-map.ts
    ├── hooks/
    │   └── useKnowledgeState.ts
    ├── lib/
    │   ├── storage.ts
    │   └── progress.ts
    ├── types/
    │   └── knowledge.ts
    ├── App.tsx
    └── main.tsx
```

## 9. 实现顺序

1. 建立 React、TypeScript、Vite 基础项目。
2. 根据 JavaGuide HTML 整理第一版知识地图数据。
3. 实现首页分类卡片和总体进度。
4. 实现分类详情页及原始目录层级。
5. 实现学习状态、面试标记、笔记和 localStorage。
6. 实现搜索、优先级筛选、状态筛选和面试暴露筛选。
7. 根据设计图进行桌面端和移动端视觉调整。

## 10. 第一阶段边界

第一版暂不实现：

- 用户登录和多设备同步；
- 后端 API 和数据库；
- AI 自动生成学习计划；
- 间隔重复算法；
- JavaGuide 内容复制或自动总结；
- 复杂的面试分析和数据报表。

